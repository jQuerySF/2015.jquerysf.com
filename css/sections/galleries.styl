// Galleries section

.galleries
  padding: 0
  background: linear-gradient(to right, $teal 50%,
                                        $blue 50%)
  text-align: left
  color: white

  .column
    padding: 40px

  // Single column, each column with a background color
  @media screen and (max-width: $small-width)

    .column
      &:first-child
        background: $teal
      &:last-child
        background: $blue

  // Two columns wide
  @media screen and (min-width: $small-width + 1px)

    .column
      width: 50%